# Vercel Postgres adds these automatically when storage is connected.
# For local development, copy the values from Vercel if you want database access locally.
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=
